{
  "private": true,
  "type": "module",
  "scripts": {
    "build:wasm": "GOOS=js GOARCH=wasm go build -trimpath -ldflags=\"-s -w\" -o ./src/demo.wasm ./wasm && cp \"$(go env GOROOT)/lib/wasm/wasm_exec.js\" ./src/wasm_exec.js",
    "build:examples": "node scripts/build-examples.mjs",
    "build": "npm run build:examples && npm run build:wasm && vite build",
    "dev": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "alpinejs": "^3.15.0"
  },
  "devDependencies": {
    "@tailwindcss/forms": "^0.5.10",
    "autoprefixer": "^10.4.22",
    "postcss": "^8.5.6",
    "tailwindcss": "^3.4.17",
    "vite": "^7.2.4",
    "wrangler": "^4.90.1"
  }
}
